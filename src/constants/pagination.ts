/**
 * Số bản ghi mỗi trang cho mọi bảng danh sách trong CMS.
 *
 * Trước đây mỗi trang tự đặt một con số (10, 15, 20, 50) nên cùng một thao tác lại ra số
 * dòng khác nhau tuỳ màn hình. Mọi nơi phân trang đều phải lấy từ đây.
 */
export const ADMIN_PAGE_SIZE = 10;
